---
# readiness 검사 전에 tasks/main.yml의 flush_handlers가 이 재시작을 완료한다.
# 따라서 설정 변경 뒤 검사는 항상 새 K3s 프로세스를 대상으로 한다.
- name: Restart K3s
  ansible.builtin.systemd_service:
    name: k3s
    state: restarted
    daemon_reload: true
