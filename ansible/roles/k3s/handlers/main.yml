---
# 설정 변경 알림을 받으면 K3s를 재시작해 설정을 다시 읽게 한다.
# tasks의 flush_handlers에서 실행되며, API 수신·노드 Ready는 재시작 뒤 별도로 기다린다.
- name: Restart K3s
  ansible.builtin.systemd_service:
    name: k3s
    state: restarted
    daemon_reload: true
