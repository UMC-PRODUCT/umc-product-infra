[defaults]
inventory = inventories/idc/hosts.yml
roles_path = roles
# 첫 실행 전에 서버 키를 직접 확인해 known_hosts에 등록하며, 이후 키 변경도 실패로 처리한다.
host_key_checking = True
interpreter_python = auto_silent
retry_files_enabled = False
nocows = True

[ssh_connection]
pipelining = True
