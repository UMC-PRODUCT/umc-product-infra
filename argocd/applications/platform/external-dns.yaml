# ExternalDNS — opted-in Ingress의 정확한 host만 Route 53과 동기화한다.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-dns
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: external-dns
  source:
    repoURL: https://kubernetes-sigs.github.io/external-dns/
    chart: external-dns
    targetRevision: 1.21.1
    helm:
      # Ingress만 source로 사용하므로 DNSEndpoint CRD는 설치하지 않는다.
      skipCrds: true
      valuesObject:
        fullnameOverride: external-dns
        image:
          tag: v0.21.0@sha256:f53faaf71cb270d1ca9dce6ea0c94bfebf1a18696263487f0fbc74b9bf2bd7ff
        provider:
          name: aws
        sources: [ingress]
        # A record와 TXT registry ownership만 허용한다.
        managedRecordTypes: [A, TXT]
        domainFilters: [university.neordinary.com]
        # Ingress가 사용하는 stable annotation namespace를 ExternalDNS에도 명시한다.
        annotationPrefix: external-dns.kubernetes.io/
        annotationFilter: external-dns.kubernetes.io/managed-by=umc-infra
        policy: sync
        registry: txt
        txtOwnerId: umc-infra-idc
        txtPrefix: _external-dns.
        interval: 1m
        triggerLoopOnEvent: true
        extraArgs:
          # Route 53의 university.neordinary.com Hosted Zone ID(비밀값 아님).
          - --zone-id-filter=Z02258617BWIPPUVEV88
          - --ingress-class=traefik
          - --aws-zone-type=public
          # ExternalDNS가 이 IDC public IPv4 외의 target을 만들지 못하게 제한한다.
          - --target-net-filter=172.198.75.88/32
        env:
          - name: AWS_DEFAULT_REGION
            value: ap-northeast-2
          - name: AWS_ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: route53-credentials
                key: access-key-id
          - name: AWS_SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: route53-credentials
                key: secret-access-key
        resources:
          requests: { cpu: 50m, memory: 64Mi }
          limits: { memory: 128Mi }
  destination:
    server: https://kubernetes.default.svc
    namespace: external-dns
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
